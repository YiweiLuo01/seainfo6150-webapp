import React from "react";
import pageStyle from "./Footer.module.css";

const Footer = () => (
    <footer className={pageStyle.footer}>
        <address className={pageStyle.name}>©2019, April Bingham</address>
    </footer>
);

export default Footer;